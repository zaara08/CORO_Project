from setuptools import setup, find_packages 

setup(
    name='se3net',
    version = '0.1',
    description = 'Predicts image from current image and action',
    author='Alisha',
    author_email='elonmusk@gmail.com',
    url='random.com',   
    packages=find_packages(), # Automatically find all packages in the directory
    install_requires=[],
    # entry_points = {},
    # classifiers
    # Python _requirements
)